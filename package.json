{
  "name": "github-id-script",
  "version": "1.0.0",
  "description": "A script to add people's github id's to their domain data, removing the need for updating the person's username every time.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
